import React from "react";

function Example() {
    // fragment use multiple element use 
  return (
     <React.Fragment>
    <header className="App-header"> 
      
       
        <h1>This is Example(Parent) Component React Fragment</h1>
        <h1>This is Example(Parent) Component React Fragment</h1>
        <h1>This is Example(Parent) Component React Fragment</h1>

      </header>
      </React.Fragment>
  );
}

export default Example;
